import { WalletBalance } from "../types";

// I see at the 'balances.filter' line so I'm sure it's an array with WalletBalance type
const useWalletBalances = (): WalletBalance[] => {
  return [];
};

export default useWalletBalances;
